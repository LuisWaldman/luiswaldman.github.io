// JavaScript source code
if (!(localStorage["poemas_cargados"]))
    localStorage["poemas_cargados"] = [];

var ListaPoemas

var CtrArchivoPoema =
{
    guardarPoema: function (nombre)
    {

    },
    cargarPoema: function (nombre) {

    }
}
